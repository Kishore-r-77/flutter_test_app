import 'package:flutter/material.dart';

class BenefitList extends StatelessWidget {
  const BenefitList({super.key, required this.benefitLists});
  final dynamic benefitLists;
  @override
  Widget build(BuildContext context) {
    print(benefitLists);
    return Container();
  }
}
